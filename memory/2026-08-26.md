
## 13:35 UTC — Brain "dead" alert storm diagnosed & watchpet hardened
- Morning alerts 09:34–12:18 were REAL: brain PID 27592 was dead, log idle 16h. start_brain_clean.cmd restart at ~12:18 UTC brought brain back (PID 25112, restart count 126). Log ticking fine since.
- 13:04 "brain PID 25112 dead" alert was FALSE POSITIVE: brain was healthy the whole time.
- Root cause: tools/watchpet_brain_alive.py `_alive()` shelled out PowerShell+Get-CimInstance with 10s timeout; any error/timeout == "dead". Also dedup compared exact msg text; incrementing "idle N min" counter re-paged every 5 min during one incident.
- Fix: psutil-based check (definitive True/False), errors -> None = warn only, no page. Dedup now keyed on issue kinds not text. Verified: bogus pid->False, non-brain pid->False, live brain->True, script exit=0.

## 17:00 — VPS migration kit built (user wants bot off local CPU, on a free server)
- Reality: stack is Windows-only (MT5+EA+brain+executor+dashboard). No always-free Windows VPS exists; Oracle free tier is Linux/ARM = MT5 won't run. Broker = Octa (bases/OctaFX-Demo) -> Octa free VPS is the best free route.
- Built outputs/vps_migration/: package_stack.ps1 (builds trendmaster_stack.zip, excludes venv/logs/tv_alert_setup 574MB; includes config/.env + brain_state.json persistent state), server_bootstrap.ps1 (fresh-Windows one-shot: py3.11+venv+deps+EA copy+start all+health verify), MIGRATION_CHECKLIST.md (safe cutover: stop local stack BEFORE VPS trades, never both at once).
- Zip final: 26.9 MB at outputs/vps_migration/trendmaster_stack.zip. Awaiting user's RDP creds of chosen server to deploy.
