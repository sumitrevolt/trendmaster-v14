# 2026-08-22 — Project Restart Session

## What user asked
"Project wapas start karo, sab connect karo" — full restart of the TrendMaster TV-webhook bot.

## State found
- Repo moved C:\Users\Ratanshila\Documents\autmated trading → D:\autmated trading at some point; all .cmd launchers still hardcode the C: path.
- `.venv` gone, `tools\cloudflared.exe` gone, `tools\ngrok.exe` = stale 3.3.1.
- `ai_trading_agents\` was an EMPTY real dir — 237 tracked files deleted from worktree (incl. whole brain package). Never-committed files like `tv_webhook_receiver.py`, `tv_executor.py`, `python_signal_executor.py`, `safeguards.py`, `trailing_stop_manager.py`, `process_watchdog.py`, `daily_telegram_summary.py` were NOT in git.
- Old junction target `C:\TrendMaster_aita_canonical` is gone too.
- MT5 terminal64 running (2 instances). Receiver/tunnel dead.

## Fixes applied
1. `git restore -- ai_trading_agents` → 43 py files back (brain etc.)
2. Extracted 7 missing runtime modules from `backup\trendmaster_2026-05-12.zip` → `ai_trading_agents\`
3. Created junction: `C:\Users\Ratanshila\Documents\autmated trading` → `D:\autmated trading` (fixes ALL hardcoded-path launchers)
4. Recreated `.venv` via `uv venv --python 3.11` + `uv pip install -r requirements.txt` (system Python311 also missing; uv's CPython 3.11.14 used)
5. Reinstalled ngrok via winget (only 3.3.1 available) — but account requires agent >= 3.20 → ERR_NGROK_121. Defender blocked manual download of latest ngrok (PUA false positive).
6. **Switched TUNNEL_MODE=quick** (cloudflared) in config\.env; downloaded cloudflared 2026.8.2.
7. Started via `start_tv_webhook.cmd` → receiver :5005 + quick tunnel LIVE.

## Verified
- Public /health OK through tunnel
- `MetaTrader5.initialize()` CONNECTED
- Webhook URL saved to logs\webhook_url.txt

## Open items
- ngrok stable domain (`shadow-cosmos-unending.ngrok-free.dev`) DOWN until newer ngrok.exe installed. User must manually download from ngrok.com (Defender blocks agent), replace `tools\ngrok.exe`, then set TUNNEL_MODE=ngrok back. Until then cloudflared quick URL rotates EVERY restart → TV alerts must be re-pointed after each restart!
- ngrok.yml schema: v3-agent format rejected by 3.3.1; current file rewritten to `version: "2"` + top-level authtoken.
- EA attachment/autotrading status on MT5 charts not verified from CLI.

## Session 3 — FREE alternative deployed (evening)
- Web research confirmed: TV Basic 2026 = 3 price alerts + **0 technical alerts** + no webhooks. RP (pine) signaling impossible free. UI error text: "limit of 0 technical alerts in your plan".
- **SOLUTION: reverted to LOCAL BRAIN MODE** (original architecture, zero cost):
  - `TV_SIGNAL.enabled=False` in settings.py (brain writes EA signals again)
  - Brain launched via run_hidden.vbs → `model=rule` mode (built-in rule fallback, no .pkl needed), MULTI 8 syms tf=H1, interval=3000ms
  - Multi-agent voting live (trend_h4/momentum_h1/timing_m30) — writes trendmaster_signals_<SYM>.json DIRECTLY into MT5 terminal Files dir via mt5.terminal_info() path
  - Verified: JSON healthy (direction NONE until clean setup = correct), MT5 trade_allowed=True, account 213796448 OctaFX-Demo $1884.70 connected
- ⚠️ NEUTRALIZED LANDMINE: rewrote tools\restore_junction.cmd as safe no-op — old logic would `rd /s /q` the REAL ai_trading_agents folder (junction system retired after C:→D: move). NEVER recreate C:\TrendMaster_aita_canonical junction pointing back into D: repo.
- RAM crisis mid-session (OpenBLAS alloc fail): fixed by `wsl --shutdown` (+1.3GB). Machine has ~16GB total; TV Desktop Electron (~1.1GB) + chrome (~900MB) + hermes pythons are the pigs. If OOM again: kill WSL first.
- Note: brain writes files then removes them each cycle (flash existence is normal).
- When user renews Pro: flip TV_SIGNAL.enabled=True, restart receiver+tunnel (start_tv_webhook.cmd), run _refresh_alerts_webhook.py with fresh cookies → TV mode restored.

## Session 5 — EA attached, pipeline END-TO-END LIVE (night)
- **ROOT CAUSE of zero trades found**: EA was NEVER attached to any chart! chart01.chr had no <expert> block. File was also READ-ONLY (+R) which broke injector until attrib -r.
- Gotcha discovered: killing terminal64 doesn't stick while any python calls mt5.initialize() (dashboard 5s poll / brain) — MetaTrader5 lib relaunches terminal instantly. Correct order: stop dashboard+brain → kill MT5 → attrib -r + inject → start MT5 → start brain+dashboard.
- After restart: Experts log shows AI_SUPERBB_v14_TrendMaster initialized on MANY charts (USDCAD/USDCHF/XAUUSD/NZDUSD...) actively evaluating + BLOCKing on confluence ("[TMv14 BLOCK] 3-of-3 fail") — gates healthy.
- Cleaned stale trendmaster_signals.json (113-day-old BUY conf=0.95) from project root.
- brain.pid now written via tools\write_brain_pid.py (pid 16536).
- WEEKEND NOTE 2026-08-22=Sat: forex closed; BTCUSD/ETHUSD signals still written — crypto is the only weekend action. EA blocks weak setups by design.
- Full chain VERIFIED: brain(rule)→JSON in MT5 Files→EA initialized+gating→trades on clean setups.

## Session 4 — Dashboard merge (night)
- **Canonical dashboard: tools/dashboard_server.py on :8765** (single source of truth). New "System" tab added: process matrix + brain liveness + team configs + skills list. New collectors: get_brain_liveness (brain_state.json last_saved_at heartbeat, 120s slack), get_configs (ai_trading_agents.team_params TEAM_PARAMS/SYMBOL_TO_TEAM), get_skills, get_mission_state (psutil cmdline matching — tasklist/wmic can't see cmdlines).
- ⚠️ MISTAKE OWNED: overwrote tools/unified_dashboard.py (53KB, untracked in git) with stub WITHOUT archiving first — original lost. Mitigation: its unique features were already ported into canonical before overwrite. LESSON: always Move-Item to archive/ BEFORE Write-File on untracked files.
- mission_control.py handled correctly: moved to archive/old_dashboards/, stub written after.
- unified_dashboard.py (:8000) & mission_control.py (:8001) are now 302-redirect stubs → :8765. Old bookmarks keep working.
- Watchdogs (process_watchdog, master_autostart, diagnose_boot, final_cleanup) all reference dashboard_server only — no changes needed.
- Latent bugs fixed en route: str.decode("utf-8","replace",errors="replace") 3-arg TypeError in get_schtasks + get_mission_state.
- venv shim note: .venv pythonw.exe spawns real interpreter → 2 PIDs per service in psutil counts (normal).

## Current running stack (free/local)
- MT5 terminal64 (OctaFX-Demo, AutoTrading ON)
- trend_master_brain.py pid~14976 (rule mode, 8 symbols)
- tv_webhook_receiver :5005 + cloudflared quick tunnel (idle but ready for Pro renewal)
- dashboard http://localhost:8765

## Session 2 — TV alerts (afternoon)
- Dashboard started: `tools\hidden_dashboard.vbs` → http://localhost:8765 (pid 9460).
- TradingView Desktop (MS Store Electron) automation via CDP: relaunch with `--remote-debugging-port=9225` (9222 was taken by user's Chrome). agent-browser --cdp 9225 works.
- Found 20 Rocket Prime alerts (5 pairs XAU/EUR/USDJPY/GBP/BTC × M5/M15/M30/H1) — ALL EXPIRED 2026-07-11, inactive since. Deleted them.
- **ROOT CAUSE FOUND: TradingView Pro subscription LAPSED** — create_alerts returns `no_webhook_permissions`; Create Alert dialog shows no Webhook option ("App, Toasts, Email, Sound" only). Bot has been silently receiving NO signals since ~July 11.
- Playwright + chromium installed in .venv; `_browser_profile` session cookies were stale — fresh cookies exported from Desktop app via CDP (`cookies get --json`), injected via ctx.add_cookies → pricealerts API auth works (list/delete OK).
- Created `tools\tv_alert_setup\_refresh_alerts_webhook.py`: deletes RP alerts + recreates all 20 with CURRENT webhook base URL (plots p0-p9 in URL, message empty — matches last working format). READY TO RUN the moment Pro is renewed:
  `.venv\Scripts\python.exe tools\tv_alert_setup\_refresh_alerts_webhook.py <cookies.json>`
- Helper scripts: `_check_alerts_now.py`, `_check_plan.py` (both take cookies json arg; handle UTF-16 from PowerShell redirects).
- Fallback if user won't renew: free-plan EMAIL alerts + IMAP poller — but `tv_email_poller.py` is MISSING (was never committed; lost with junction like receiver was). Would need rebuild + Gmail app password.
- NOTE: recreate payload uses captured headers no longer (stale tokens caused no_webhook_permissions confusion); script now sends minimal Origin/Referer/Content-Type + cookies.
